[project]
name = "billing"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "httpx==0.27.0",
    "pydantic>=2.6",
]

[project.optional-dependencies]
dev = ["pytest==8.2.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
